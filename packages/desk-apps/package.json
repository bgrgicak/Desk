{
  "name": "@agent-desk/desk-apps",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Desk-shipped built-in apps. Synced to ~/Desk/.apps/ on server start and mounted into every sandbox at /opt/desk-apps/.",
  "scripts": {
    "build": "node scripts/build-apps.mjs",
    "typecheck": "node scripts/build-apps.mjs --typecheck"
  }
}
