/// <reference types="vite/client" />

// Inlined by Vite's `define` (see vite.config.ts) from packages/app/package.json#version.
// Available in the bundle at build time and in dev (resolved to the
// version at the moment `vite dev` started).
declare const __APP_VERSION__: string
