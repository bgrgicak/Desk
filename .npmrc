# engine-strict was previously needed to prevent better-sqlite3 from being
# compiled under the wrong Node major (a runtime crash at startup). Native
# modules have been replaced with node:sqlite (built-in) and hash-wasm
# (WASM), so the strict version gate is no longer necessary.
