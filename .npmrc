# Enforce the Node version pinned by .nvmrc + package.json#engines.
# Without this, npm install/ci treats engines as advisory and silently
# accepts mismatched majors — which produces native bindings
# (better-sqlite3) that crash at runtime with NODE_MODULE_VERSION
# mismatches when the running Node major doesn't match the build major.
engine-strict=true
